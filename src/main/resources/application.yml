spring:
#  profiles:
#    active: @spring.profiles.active@
#  config:
#    activate:
#      on-profile: local

  datasource:
    url: jdbc:postgresql://localhost:5432/my_diary
    username: bel
    password: bel2

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  port: 8082
